<% div_for (comment, :class => 'post') do %>
  <div class="about">
    Posted <span class="timestamp"><%= time_ago_in_words(comment.created_at) %></span> ago by <span><%= h(comment.name) %></span>
  </div>
  <p><%= h(comment.message) %></p>
<% end %>